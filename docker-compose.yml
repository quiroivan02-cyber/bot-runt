services:
  bot-runt:
    build: .
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./debug:/app/debug
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
